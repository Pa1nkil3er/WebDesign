// let positive = 0;
// let negative = 0;
// let zero = 0;
// let paired = 0;
// let unpaired = 0;
// for (let i = 0; i < 10; i++) {
// 	let n = +prompt("Введіть число: ");
// 	if (n != NaN) {
// 		if (n > 0) { positive++; }
// 		if (n < 0) { negative++; }
// 		if (n == 0) { zero++; }
// 		if (n % 2 === 0) { paired++; }
// 		if (n % 2 !== 0) { unpaired++; }
// 	}
// }
// alert(`К-ть додатніх: ${positive}, к-ть від'ємних: ${negative}, к-ть нулів ${zero}, к-ть парних ${paired}, к-ть непарних ${unpaired}`);